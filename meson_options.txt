option('bundle_dwarfs',
  type: 'boolean',
  value: true,
  description: 'Bundle DwarFS tools (dwarfsextract) during build'
)

option('bundle_zsync',
  type: 'boolean',
  value: true,
  description: 'Bundle zsync2 tool for delta updates during build'
)

option('bundle_unsquashfs',
  type: 'boolean',
  value: true,
  description: 'Bundle unsquashfs tool for AppImage extraction during build'
)

option('bundle_sas',
  type: 'boolean',
  value: true,
  description: 'Bundle sas (simple-appimage-sandbox) and squashfuse for the AppImage sandbox. Requires bundle_dwarfs, which also supplies the dwarfs FUSE driver sas needs.'
)
